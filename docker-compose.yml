services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:5000
    image: changebody-fe:latest
    container_name: changebody-fe
    ports:
      - "3001:80"
    restart: unless-stopped
